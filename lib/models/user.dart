class User {
  int id;
  DateTime createdAt;
  String name;
  String avatar;

  User(
      {required this.id,
      required this.createdAt,
      required this.name,
      required this.avatar});
}
